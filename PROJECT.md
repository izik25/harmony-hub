# SONA — מסמך מצב הפרויקט

עדכון אחרון: 2026-08-12

מסמך זה מתעד את מה שיש היום ב-SONA: איך המערכת בנויה, אילו פיצ'רים עובדים באמת (לא דמו), איך מריצים הכל, ואיפה יש עוד עבודה. המטרה שתמיד יהיה כאן מקום אחד לחזור אליו ולהבין את התמונה המלאה.

## מה זה SONA

אפליקציית סושיאל למוזיקה בסגנון TikTok/קריוקי: משתמשים מקליטים שירה (עם או בלי פלייבק קריוקי), מעבדים את ההקלטה עם אפקטי סאונד אמיתיים, מפרסמים לפיד, מתחרים בתחרויות, שולחים מתנות וירטואליות, שולחים הודעות פרטיות, ובעתיד גם משדרים לייב.

**כתובת חיה:** https://harmony-hub-olive.vercel.app
**ריפו:** https://github.com/izik25/harmony-hub

## סטאק טכנולוגי

- **פרונטאנד**: React 19 + TanStack Start (Router/Query) + Vite, Tailwind
- **בקאנד**: TanStack Start server functions (`createServerFn`) — אין שרת API נפרד
- **DB**: Postgres דרך Drizzle ORM (`drizzle-orm` + `pg`), schema-first ב-`src/db/schema.ts`
- **אחסון קבצים**: Vercel Blob (הקלטות, תמונות, קבצי אודיו מעובדים)
- **אימות**: מייל+סיסמה, bcrypt, session token ב-cookie httpOnly (בלי JWT)
- **DSP אמיתי בסטודיו**: Web Audio API / Tone.js (Pitch Shift, EQ3, Compressor, Reverb קונבולוציה) — לא ML, אפקטים אמיתיים על הגל
- **שידור חי**: LiveKit — **עדיין לא מוגדר** (חסר credentials, ראו "מה חסר" למטה)
- **תרגום**: i18next, עברית/אנגלית/ערבית מלאות, כולל RTL וזיהוי שפה בצד השרת

## איך זה בנוי

### ניתוב (routing)
כל מסך תחת `src/routes/*.tsx` (קובץ = ראוט, מוסכמות TanStack Router). מסכים עיקריים: פיד (`index`), גילוי (`explore`), תחרויות (`competitions`, `competitions_.$id`), לייב (`live`, `live_.$roomId`), ארנק (`wallet`), הקלטה (`record`), סטודיו (`studio`), העלאה (`upload`), הודעות (`messages`, `messages_.$conversationId`), פרופיל (`profile`, `profile_.$handle`), label hub, התראות, התחברות/הרשמה.

### לוגיקת שרת
כל הלוגיקה העסקית תחת `src/functions/*.ts` (auth, posts, wallet, competitions, messages, notifications, label, live, karaoke, uploads, search). כל פונקציה היא `createServerFn` שרצה בצד השרת בלבד ונקראת מהקליינט כמו קריאת RPC רגילה.

### מסד נתונים
**חשוב: יש שני מסדי נתונים נפרדים!**
- **לוקאלי** (לפיתוח): Postgres מקומי על המחשב שלך (`postgresql-x64-16`, DB בשם `sona`), מוגדר ב-`.env`
- **פרודקשן** (האתר החי): Neon Postgres בענן, מחובר אוטומטית ל-Vercel דרך `DATABASE_URL`

זה אומר: אם משנים סכמה או מוסיפים תוכן (כמו שירי קריוקי), **צריך להריץ את זה פעמיים** — פעם על ה-DB המקומי (`npm run db:push` / `npm run db:seed`) ופעם על Neon (אותה פקודה עם `DATABASE_URL` שמוצבע ל-Neon, ראו "פקודות שימושיות" למטה).

> ⚠️ **מלכודת אמיתית שכבר תפסה אותנו**: אל תיצור/תשאיר קובץ בשם `.env.local` בשורש הפרויקט. קובץ כזה נטען על ידי Vite עם **עדיפות גבוהה יותר מ-`.env`**, כך ש-`vercel link`/`vercel env pull`/`vercel blob create-store` (שיוצרים קובץ `.env.local` כזה עם משתני **פרודקשן**) גורמים לשרת הפיתוח המקומי לפנות בלי אזהרה ל-Neon/Blob האמיתיים במקום ל-Postgres המקומי. זה קרה בפועל וגרם לכמה הקלטות/דראפטים "לוקאליים" בבדיקה להיכתב בטעות לפרודקשן (נוקה בדיעבד). הפתרון: אם צריך משתני Neon/Blob לפקודה חד-פעמית, שלוף אותם מ-`.vercel/.env.production.local` במקום — קובץ נפרד, בתוך `.vercel/`, ש-Vite לא סורק, כך שאין התנגשות. אם `.env.local` נוצר שוב (למשל אחרי `vercel link` נוסף), פשוט מחק אותו.

### אחסון קבצים
`uploadMedia` (ב-`src/functions/uploads.ts`) שולח לאחסון בענן אמיתי — Vercel Blob — כי לשרתים של Vercel אין דיסק מקומי כותב. אם רצים לוקאלית בלי `BLOB_READ_WRITE_TOKEN`, זה נופל אוטומטית לכתיבה לתיקיית `public/uploads/` המקומית (לנוחות פיתוח בלבד).

## פיצ'רים — מה עובד באמת

| פיצ'ר | סטטוס |
|---|---|
| הרשמה/התחברות/session אמיתי | ✅ עובד |
| פיד עם לייקים/קומנטים/שיתופים/עוקבים אמיתיים | ✅ עובד |
| פיד: השיר של הפוסט שנמצא במסך באמת מתנגן תוך כדי גלילה (autoplay מושתק → הקשה מבטלת השתקה, משתיק/מפעיל את הבא בתור, פס התקדמות אמיתי) | ✅ עובד — `IntersectionObserver` ב-`index.tsx`, ראו היסטוריה למטה |
| ארנק מטבעות וירטואליים (קנייה/משיכה/מתנות) | ✅ עובד — אין כסף אמיתי, כלכלה סגורה בכוונה |
| תחרויות + הצבעה | ✅ עובד |
| הודעות פרטיות (DM) בין משתמשים | ✅ עובד |
| הקלטה עם מיקרופון אמיתי + ויזואליזציה | ✅ עובד |
| סיום הקלטה עובר ישר לאולפן ה-AI (בלי כפתור "שמור" נפרד) — שם כל העריכה: איזון ווקאל/פלייבק, DSP בזמן אמת, ואז שמירה/פרסום/שליחה לתחרות | ✅ עובד |
| בחירת שיר קריוקי מהמאגר + וידאו מנוגן בסנכרון עם ההקלטה | ✅ עובד |
| מיקס פלייבק הקריוקי + מיקרופון בהקלטה השמורה, עם ניקוי רעש/דחיסה (שיר שלם ונקי, לא רק ווקאל גולמי) | ✅ עובד — רינדור אופליין דרך `OfflineAudioContext` אחרי ההקלטה (לא מיקס חי), ראו `src/lib/mix-recording.ts` |
| כוונון עוצמת קול מול עוצמת פלייבק אחרי ההקלטה + "עדכן מיקס" בלי להקליט מחדש | ✅ עובד — סליידרים ב-`record.tsx`, מריצים שוב את `processRecording` על ההקלטה הגולמית השמורה |
| השהיה/המשך הקלטה (בלי לאבד את מה שכבר הוקלט) | ✅ עובד |
| סטודיו: אפקטי DSP אמיתיים בזמן אמת (לא AI) + ייצוא + פרסום | ✅ עובד |
| Label Hub (חיפוש כישרונות, אודישנים) | ✅ עובד |
| התראות | ✅ עובד |
| עברית/אנגלית/ערבית מלאות כולל RTL | ✅ עובד |
| שידור לייב (LiveKit) | ❌ **לא מוגדר** — חסרים credentials, ראו למטה |
| "פרסום לכל הפלטפורמות" — חלון אחרי פרסום בתוך האפליקציה, לבחור אליו לפרסם גם (יוטיוב/טיקטוק/אינסטגרם/ספוטיפיי/אפל מיוזיק/סאונדקלאוד) | ⚠️ **בנוי, לא מוגדר** — הקוד עובד מקצה לקצה אבל דורש credentials פר-פלטפורמה שעדיין לא הוזנו, ראו למטה |

## פרסום לכל הפלטפורמות (Publish everywhere)

כפתור "Publish" בתוך האפליקציה פותח אחריו חלון לבחירת פלטפורמות חיצוניות לפרסם אליהן גם:

- **יוטיוב Shorts / טיקטוק / אינסטגרם Reels** — חיבור OAuth אמיתי (מסך "Connect" בחלון), ואז אפלוד אמיתי ל-API של אותה פלטפורמה. כיוון שהקלטות ב-SONA הן אודיו בלבד, לפני ההעלאה נבנה בדפדפן (Canvas + MediaRecorder) קליפ וידאו אנכי מתמונת הקאבר + השם + האודיו האמיתי — ראו `src/lib/video-synthesis.ts`.
- **ספוטיפיי / אפל מיוזיק / סאונדקלאוד** — לאף אחת מהן אין API ציבורי להעלאת שיר כאמן עצמאי, אז זה לא אפלוד אמיתי: מוריד את קובץ השיר, מעתיק את השם ללוח, ופותח את דף ההעלאה של הפלטפורמה בטאב חדש כדי שהמשתמש ישלים ידנית.

**קבצים מרכזיים**: `src/lib/social-platforms/` (מודול OAuth+אפלוד לכל פלטפורמה + `share-only.ts` לשלוש הפלטפורמות ללא API), `src/functions/platforms.ts` (server functions), `server.ts` (`/api/connect/:platform/start` ו-`/callback` — טיפול גולמי ב-Request/Response, לא דרך `createServerFn`, כי צריך שליטה מלאה ב-redirect/cookies, באותו סגנון שכבר קיים שם ל-`/api/blob-upload`), `src/components/PublishEverywhereModal.tsx`.

### איך מפעילים כל פלטפורמה

כל פלטפורמה עצמאית — בלי ה-credentials שלה היא פשוט מוצגת כ"עדיין לא הוגדר" בחלון, שאר האפליקציה לא מושפעת. צריך למלא ב-`.env` (ולוקאלית **וגם** ב-Vercel לפרודקשן):

1. **יוטיוב** — Google Cloud Console → פרויקט חדש → הפעל "YouTube Data API v3" → OAuth Client ID (Web application) → `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`.
2. **טיקטוק** — TikTok for Developers → אפליקציה עם מוצר "Content Posting API" → `TIKTOK_CLIENT_KEY` / `TIKTOK_CLIENT_SECRET`. **הערה חשובה**: כל עוד האפליקציה לא עברה אישור (audit) של טיקטוק, אפלוד לא מתפרסם ישירות — הוא נוחת ב-inbox של הטיקטוק של המשתמש כטיוטה, והוא צריך לפתוח את טיקטוק ולאשר פרסום בעצמו.
3. **אינסטגרם** — Meta for Developers → אפליקציה עם "Instagram Graph API" → `META_APP_ID` / `META_APP_SECRET`. **דרישה**: החשבון שמתחברים איתו חייב להיות Instagram Business/Creator שמקושר לדף פייסבוק — חשבון אינסטגרם רגיל לא יעבוד.

בכל אחת, ה-redirect URI שצריך לרשום אצל הפלטפורמה הוא `{הכתובת שלך}/api/connect/{youtube|tiktok|instagram}/callback` — גם עבור `http://localhost:8080` (או הפורט שרץ עליו לוקאלית) וגם עבור הכתובת החיה.

### מגבלה ידועה: וידאו לאינסטגרם

אינסטגרם דורשת MP4 (H.264/AAC) בלבד. כרום מודרני יודע להקליט MediaRecorder ישירות כ-MP4, אז זה עובד — אבל פיירפוקס/ספארי לא, ונופלים ל-WebM שאינסטגרם דוחה. המשתמש שמפרסם צריך כרום בשביל אינסטגרם ספציפית (יוטיוב וטיקטוק מקבלים גם WebM).

## חשבונות דמו

כל החשבונות הבאים קיימים גם בלוקאלי וגם בפרודקשן (Neon), סיסמה אחידה `demo1234`:

- `nova@demo.sona` — Nova Ray
- `kai@demo.sona` — Kai Aster
- `nyx@demo.sona` — DJ Nyx
- `lila@demo.sona` — Lila Moon
- `atlas@demo.sona` — Atlas Vex

## איך מריצים לוקאלית

```bash
npm install
npm run db:push      # מריץ את הסכמה על ה-Postgres המקומי (.env)
npm run db:seed       # ממלא נתוני דמו
npm run dev            # מריץ על localhost:8080
```

## איך מוסיפים שירי קריוקי

1. שים קובץ MP4 (עם המילים כבר צרובות בווידאו) בתיקייה `public/karaoke/`
2. תן לו שם בפורמט `אמן - שם השיר.mp4` (זה נהיה אוטומטית האמן/כותרת שמוצגים באפליקציה) — אפשר עברית חופשי, זה נשאר קריא בתיקייה
3. הרץ `npm run db:sync-karaoke` — זה מעלה את הקובץ בפועל ל-Vercel Blob (או לתיקייה מקומית אם אין `BLOB_READ_WRITE_TOKEN`) ומסנכרן את ה-DB בהתאם
4. **חשוב**: זה מסנכרן רק מול ה-DB/Blob שמוגדרים כרגע ב-`DATABASE_URL`/`BLOB_READ_WRITE_TOKEN`. כדי שהשיר יופיע גם באתר החי, צריך להריץ את זה גם מול Neon+Blob (ראו "פקודות שימושיות" למטה), ואז `vercel deploy --prod`.

**הערה טכנית**: בהתחלה ניסינו לשמור את קבצי הווידאו כקבצים סטטיים תחת `public/karaoke/` ולתת ל-Vercel להגיש אותם ישירות — זה התגלה כלא אמין (אותו נתיב בדיוק חזר לפעמים 200 ולפעמים 307 חזרה ל-login, גם עם שם תיקייה אנגלי בלי תווים מיוחדים). המעבר לאחסון ב-Vercel Blob (אותו מנגנון שכבר משמש להקלטות) פתר את זה לגמרי ובאופן עקבי.

## תשתית / Deployment

- **פרויקט Vercel**: `harmony-hub` (תחת `izik25s-projects`)
- **DB פרודקשן**: Neon Postgres, סטור בשם `neon-cerulean-ferry`, מחובר דרך אינטגרציית Marketplace
- **אחסון קבצים פרודקשן**: Vercel Blob, סטור בשם `sona-media` (public access)
- **משתני סביבה שמוגדרים ב-Vercel**: `DATABASE_URL`, `SESSION_SECRET`, `BLOB_READ_WRITE_TOKEN`, ועוד כמה שמגיעים אוטומטית מ-Neon (`PGHOST`, `PGUSER` וכו')
- **פריסה**: `npx vercel deploy --prod` מהתיקייה הראשית (בונה ופורס בפקודה אחת, ה-alias `harmony-hub-olive.vercel.app` מתעדכן אוטומטית)

## פקודות שימושיות

```bash
# לראות אילו env vars מוגדרים ב-Vercel
npx vercel env ls

# להריץ db:push / db:seed מול Neon (פרודקשן) במקום מול הלוקאלי
NEON_URL=$(grep "^DATABASE_URL=" .vercel/.env.production.local | sed 's/^DATABASE_URL="\(.*\)"$/\1/')
DATABASE_URL="$NEON_URL" npx drizzle-kit push
DATABASE_URL="$NEON_URL" npx tsx src/db/seed.ts

# סנכרון קריוקי מול Neon+Blob (פרודקשן) — צריך גם את שני המשתנים יחד
BLOB_TOKEN=$(grep "^BLOB_READ_WRITE_TOKEN=" .vercel/.env.production.local | sed 's/^BLOB_READ_WRITE_TOKEN="\(.*\)"$/\1/')
DATABASE_URL="$NEON_URL" BLOB_READ_WRITE_TOKEN="$BLOB_TOKEN" npx tsx src/db/sync-karaoke.ts

# אותו דבר עבור אודיו הדמו של 5 הפוסטים הזרועים (npm run db:seed-demo-audio מריץ מול הלוקאלי)
DATABASE_URL="$NEON_URL" BLOB_READ_WRITE_TOKEN="$BLOB_TOKEN" npx tsx src/db/seed-demo-audio.ts

# פריסה לפרודקשן
npx vercel deploy --prod
```

## מה חסר / עבודה עתידית

- **שידור לייב**: כל התשתית (טבלת `live_rooms`, `src/functions/live.ts`, מסכי `live.tsx`/`live_.$roomId.tsx`) בנויה ומחכה, אבל צריך מפתחות LiveKit אמיתיים (`LIVEKIT_URL`, `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET`) — יש טייר חינמי ב-cloud.livekit.io. בלעדיהם המסך מציג הודעת "לא מוגדר" בצורה מסודרת ולא נשבר.
- **הגנה מפני חשיפת שגיאות DB גולמיות**: אחרי התקרית שבה שגיאת SQL גולמית הוצגה למשתמש (כשה-DB בפרודקשן לא היה מחובר), הוספתי הגנה (`src/functions/safe-error.ts`) שמכסה כרגע רק את נתיב ההתחברות/session (`auth.ts`) — שזה הכי חשוף כי כל טעינת עמוד עוברת שם. שאר הפונקציות בשרת (wallet, posts, competitions וכו') עדיין לא עטופות באותה הגנה — כדאי להרחיב את זה בהמשך.
- **מאגר קריוקי**: כרגע יש 2 שירים בלבד (של איתי לוי). המשתמש מזין תוכן בעצמו — אין קטלוג מוזיקה מסחרי מחובר (בכוונה, סוגיית זכויות יוצרים).
- אזהרת קונסול לא מזיקה אחת שנשארת ב-dev בלבד (`data-tsd-source` — כלי דיבוג פנימי של Lovable, לא רץ בפרודקשן, לא משפיע על שום דבר).
- **אודיו הדמו של 5 הפוסטים הזרועים רץ כרגע רק לוקאלית** — `npm run db:seed-demo-audio` עודכן ב-DB המקומי (Postgres מקומי + `public/uploads/`), אבל עוד לא רץ מול Neon+Blob (פרודקשן). צריך להריץ את הפקודה מ"פקודות שימושיות" למעלה כדי שגם באתר החי הפוסטים של 5 חשבונות הדמו ישמיעו שיר בפועל בפיד.

## היסטוריה קצרה — מה תוקן לאחרונה

- הפרויקט התחיל כפרוטוטייפ עם נתוני דמו בלבד (`mock-data.ts`) וכפתורים שלא עשו כלום — נבנה בקאנד אמיתי מלא מאפס (DB, auth, כל 9 אזורי הפיצ'רים).
- תורגם לעברית/ערבית מלאות כולל זיהוי שפה בצד שרת (למניעת "קפיצות" שפה).
- נבנה פיצ'ר קריוקי: בחירת שיר → וידאו מסונכרן עם הקלטה.
- **תוקן**: התחברות באתר החי לא עבדה כי לא היה DB מחובר בכלל בפרודקשן (Vercel ניסה להתחבר ל-localhost של המחשב האישי) — חובר Neon Postgres אמיתי.
- **תוקן**: הקלטות לא נשמרות בפרודקשן כי לשרתי Vercel אין דיסק כתיב — חובר Vercel Blob לאחסון קבצים.
- **נוסף**: אפשרות להשהות הקלטה ולהמשיך אותה מאותה נקודה (במקום להתחיל מחדש), עם החלטה מפורשת של "המשך" מול "סיום" אחרי עצירה.
- **תוקן**: וידאו הקריוקי לא עלה על המסך בפרודקשן — קבצים סטטיים תחת `public/karaoke/` התבררו כלא אמינים ב-Vercel (307 לא עקבי חזרה ל-login). הועברו לאחסון ב-Vercel Blob, אותו מנגנון שכבר עבד טוב עבור הקלטות.
- **תוקן (תשתית)**: התגלה ותוקן באג שגרם לשרת הפיתוח המקומי "לדלוף" לפרודקשן (ראו האזהרה על `.env.local` למעלה) — כמה בדיקות "מקומיות" כתבו בטעות ל-Neon/Blob האמיתיים; נוקה בדיעבד.
- **נוסף/שופר (כמה גרסאות)**: ההקלטה עכשיו כוללת גם את פלייבק הקריוקי, לא רק את המיקרופון — כדי שיוצא שיר שלם. הגרסה הראשונה ערבבה בזמן אמת דרך Web Audio (`createMediaElementSource`/`captureStream`), אבל זה יצר בעיות (הפלייבק הפסיק להישמע למבצע, ולפעמים היו רעשי קליקים/קפיצות מהחיבור בין שני מקורות עם שעונים לא מסונכרנים). **הפתרון הסופי**: המיקרופון מוקלט לבד ונקי (בלי שום עיבוד חי), הווידאו ממשיך להתנגן ולהישמע רגיל ב-`<video>` בלי שום נגיעה, ורק *אחרי* העצירה נעשה מיקס אופליין (`OfflineAudioContext`, לא בזמן אמת) שמשלב את שני המקורות + מפעיל high-pass filter (ניקוי רעש/המהום) וקומפרסור (רמות אחידות, "אולפני") — ראו `src/lib/mix-recording.ts`. גם נוספו `noiseSuppression`/`echoCancellation`/`autoGainControl` על הבקשה ל-`getUserMedia` של המיקרופון עצמו.
- **תוקן (משמעותי, כלל-אפליקטיבי)**: `<Toaster />` של sonner מעולם לא היה מורכב (mounted) בשום מקום בעץ הרכיבים — כל קריאות `toast.success/error/info(...)` בכל האפליקציה (עשרות מקומות: שגיאות שרת, אישורי פרסום, "הועתק קישור" וכו') פשוט לא הציגו כלום למשתמש, בלי שגיאה נראית לעין. תוקן ע"י הוספת `<Toaster richColors position="top-center" />` ל-`RootComponent` ב-`__root.tsx`. אומת: toast של "Published!" עכשיו באמת מופיע אחרי פרסום מוצלח.
- **שופר (איכות הקלטה, סיבוב 1)**: נוסף noise gate אמיתי (לא רק פילטר) שמנקה רעש/הדלפת פלייבק בין משפטים (`applyNoiseGate` ב-`src/lib/mix-recording.ts`), לימיטר בסוף השרשרת (מונע קליפינג קשה שנוצר מה-makeup gain), EQ עדין לבהירות הקול (~3kHz), והנמכת עוצמת וידאו הקריוקי אוטומטית ל-35% *בזמן ההקלטה בפועל* (חוזרת ל-100% במעצור/סיום) כדי לצמצם הדלפה אקוסטית מהרמקול של הטלפון למיקרופון — ועם הודעת "מומלץ אוזניות" כשבוחרים שיר. גם נוסף timeout (fetch + רינדור + timeout כללי) כדי ש"שמור" לעולם לא יישאר תקוע/מושבת לצמיתות.
- **שופר (איכות הקלטה, סיבוב 2 — "נשמע כמו הודעה קולית")**: אחרי סיבוב 1 האוזניות פתרו את בעיית ההדלפה, אבל הקול עצמו עדיין נשמע דק/"טלפוני". שתי סיבות עיקריות טופלו: (1) `MediaRecorder` הוקלט בביטרייט ברירת מחדל נמוך יחסית — עכשיו מוגדר מפורש ל-128kbps (`audioBitsPerSecond`), ונוספו `sampleRate`/`channelCount` מועדפים ל-`getUserMedia`. (2) שרשרת ה-DSP ב-`mix-recording.ts` הורחבה: low-shelf "warmth" (~200Hz, לגוף/עומק), high-shelf רך שמרכך סיביליות/רעש חד, ושליחת ריוורב עדינה (convolution קצרה, אותו מנגנון כמו בסטודיו — `generateImpulseResponse`) שמוסיפה תחושת חלל במקום קול "צמוד למיקרופון" יבש לגמרי. אורך הרינדור האופליין גם הוארך כדי שזנב הריוורב לא ייחתך בפתאומיות.
- **נוסף (סיבוב 3 — שליטה ידנית באיזון)**: `processRecording` מקבל כעת פרמטר `MixLevels` אופציונלי (`vocalGain`/`backingGain`, ברירות מחדל 1.4/0.65 — זהות להתנהגות הקודמת). ב-`record.tsx` נוספו שני סליידרים ("עוצמת קול" 50–200%, "עוצמת פלייבק" 0–150%, מוצגת רק כשיש שיר קריוקי) וכפתור "עדכן מיקס" שמריץ שוב את `processRecording` על ה-blob הגולמי השמור (`rawBlobRef`) בלי להקליט מחדש — כך אפשר לנסות איזונים שונים ולהאזין (Preview מתעדכן אוטומטית לגרסה החדשה) עד שמתאים, בדיוק כמו בסטודיו אמיתי.
- **תוקן (משמעותי, כלל-אפליקטיבי) — הפיד לא השמיע אודיו בכלל**: לכל פוסט תמיד היה `audioUrl` אמיתי בבסיס הנתונים (השדה קיים וממולא לכל פרסום דרך `publishPost`), אבל `FeedItem` ב-`index.tsx` מעולם לא רינדר אלמנט `<audio>` — הגלילה הייתה מציגה רק רקע גרדיאנט סטטי (`PostCoverBg`) בלי קול, גם לפוסטים אמיתיים של משתמשים וגם לדמו. תוקן: `FeedSlider` עוקב עם `IntersectionObserver` אחרי איזה פוסט נמצא במסך ומעביר `active` ל-`FeedItem`, שמנגן/עוצר בהתאם (autoplay מושתק כברירת מחדל בגלל מדיניות דפדפנים, הקשה במקום כלשהו בפיד מבטלת השתקה גלובלית לכל הפוסטים הבאים — בדיוק כמו טיקטוק). נוספו גם פס התקדמות אמיתי בתחתית המסך, "נשימה" עדינה לרקע ואייקון הדיסק שמסתובב רק כשבאמת מתנגן, ורמז "הקש להפעלת קול" בפוסט הראשון. בנוסף, 5 הפוסטים הזרועים (`src/db/seed.ts`) לא היה להם בכלל `audioUrl` (מחרוזת ריקה) — נוסף `src/db/seed-demo-audio.ts` שמייצר לכל אחד מהם לופ נגינה קצר וייחודי (סינתזה מקורית לגמרי בקוד — אקורדים/בס/תופים/ריוורב פשוט, לא דגימות, אז אין סוגיית זכויות יוצרים), ומעלה אותו דרך אותו מנגנון Blob/`public/uploads` כמו קבצים אחרים. הרצה: `npm run db:seed-demo-audio` (אחרי `db:seed`).
- **תוקן (מסך הקלטה — UI)**: וידאו הקריוקי היה קטן (מרופד בתוך כרטיס) וכפתור ההקלטה הענק ישב במרכזו וכיסה את המילים. בזמן הקלטה בפועל היה כפתור יחיד ומעורפל (אייקון ריבוע, אבל בפועל רק השהה) — בלי דרך ברורה לסיים. תוקן: הווידאו נצמד לקצוות הכרטיס (ללא ריפוד), והפקדים עברו לרצועה דקה בתחתית עם שני כפתורים קטנים ונפרדים — **השהה** ו**סיום** — כך שהמילים נשארות גלויות. גם תוקן: כפתורי שמור/המשך לפרסום/שלח לתחרות היו מנוטרלים לגמרי (בלי שום הודעה) כל עוד לא הייתה הקלטה מוכנה — עכשיו תמיד לחיצים ומציגים טוסט ברור אם משהו חסר. וכפתור "פרסם" בעמוד ההעלאה דרש כותרת לא ריקה בלי שום הסבר כשהיא חסרה (השרת בכלל לא דורש כותרת, נופל ל-"Untitled" מעצמו) — הדרישה בצד הלקוח הוסרה.
- **תוקן (קריטי, כלל-אפליקטיבי) — שמירה/פרסום נכשלו על הקלטות אמיתיות (`FUNCTION_PAYLOAD_TOO_LARGE`)**: כל העלאת מדיה (`uploadMedia` ב-`src/functions/uploads.ts`) עברה כ-body של קריאת `createServerFn`, שכפופה למגבלת הגודל של פונקציית serverless ב-Vercel (בערך 4.5MB). הקלטת מיקרופון קצרה לבדיקות אף פעם לא חצתה את זה, אבל טייק אמיתי של שיר שלם — WAV לא דחוס, כ-96KB לשנייה מונו — חוצה את זה תוך פחות מדקה. זה השפיע על כל 4 נקודות ההעלאה באפליקציה (הקלטה, אולפן, העלאת קובץ, תמונת פרופיל). **הפתרון**: נוסף `/api/blob-upload` — handler גולמי (לא `createServerFn`, כי `@vercel/blob/client`'s `upload()` קורא לו ישירות בפורמט משלו) ב-`src/server.ts`, עם בדיקת session מה-cookie, שמיישם את `handleUpload` של Vercel Blob. הדפדפן מעלה עכשיו ישירות ל-Blob storage — הפונקציה שלנו רואה רק בקשת טוקן JSON קטנה, אף פעם לא את בייטים של הקובץ עצמו. `src/lib/blob-upload.ts` עוטף את זה כ-`smartUploadMedia()`, עם נפילה אוטומטית חזרה ל-`uploadMedia` המקורי כשההעלאה הישירה לא זמינה (לוקאלית, בלי `BLOB_READ_WRITE_TOKEN` — שם אין בכלל מגבלת גודל כזו). אומת בפועל בפרודקשן: הקלטה של 76 שניות (קובץ 7.3MB) נשמרה ופורסמה בהצלחה.
- **תוקן (קריטי, איכות שמע) — "הד רציני" בכל הקלטה, גם אחרי הסבבים הקודמים**: הסבבים הקודמים תיקנו רק את `mix-recording.ts` (המיקס האוטומטי), אבל האולפן (`studio.tsx`) — שאליו כל הקלטה מגיעה אוטומטית — טען כל טייק עם `autotune=40%` ו-`reverb=30%` כברירת מחדל, עוד לפני שהמשתמש נגע בסליידר. שני הבאגים האמיתיים: (1) אין שום זיהוי גובה קול באפליקציה (`pitchRef` תמיד 0), אז ה"אוטוטיון" רץ על 0 חצאי-טון — אבל `Tone.PitchShift` ממומש עם שני קווי דיליי גרעיניים שמתנדנדים עד 100ms גם ב-0 חצאי-טון, כך שערבוב wet/dry שלו נשמע כמו "פלאטר"/סלאפבק־אקו בלי שום תועלת בפועל בכיוונון גובה הקול. (2) ה-impulse response של הריוורב היה 2.5 שניות עם decay איטי (2.2) — זנב ארוך שנשמע כהד נפרד ולא כאווירה, גם ב-30% בלבד. **התיקון**: `autotune` בברירת מחדל 0 (גם ב-state הראשוני וגם ב"Auto Master", שכפה `setA(60)` בלי שום קשר לניתוח בפועל), `reverb` בברירת מחדל 10 (היה 30) עם תקרת wet מוקטנת (0.45 במקום 0.7 מתוך ה-slider), וה-impulse response קוצר ל-1.3 שניות עם decay מהיר יותר (3.2) כך שגם ריוורב מלא נשמע כחדר קטן ולא כאולם. `applyMaster` גם מקטין את טווח הריוורב שהוא מחשב (5–14 במקום 8–22).
- **שדרוג משמעותי — איחוד ההקלטה והעריכה למסך אולפן אחד**: לפני כן, סיום הקלטה נתן 4 יציאות נפרדות (Preview/שמור/המשך לפרסום/שלח לתחרות) + סקציית "איזון מיקס" + כפתורי DSP שלא עשו כלום עד שעברת לינק לאולפן עם ערכיהם כ-URL params. עכשיו סיום הקלטה עובר **ישר** לאולפן (בלי "שמור" נפרד), והאולפן הוא המקום היחיד לכל העריכה: play/pause עם DSP חי (AutoTune/רעש/ריוורב/EQ/דחיסה/מהירות), ואז Export/המשך לפרסום/שלח לתחרות. גם איזון ווקאל/פלייבק עבר לשם: `record.tsx` מעלה עכשיו גם את ההקלטה הגולמית הנקייה (עמודות חדשות `raw_vocal_url`/`backing_track_url` בטבלת `posts`), כך שסקציית "איזון מיקס" החדשה באולפן יכולה לערבב מחדש מהמקור (דרך `processRecording` הקיים) ולעדכן את ה-draft בלי להקליט מחדש. תוקן גם באג סמוי: אפקט ה-DSP לא הוחל מחדש אחרי טעינת שרשרת Tone חדשה (רק כשסליידר זז) — זה לא הזיק כל עוד לאולפן היה audioUrl קבוע אחד, אבל עכשיו ש"עדכן מיקס" מחליף את ה-audioUrl בזמן אמת זה היה מאפס בשקט את כל כפתורי ה-DSP. תוקן ע"י הפעלת `applyParams` מיד אחרי כל טעינת שרשרת.
